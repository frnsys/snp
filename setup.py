from setuptools import setup, find_packages

setup(
    name='snp',
    version='0.0.1',
    description='freeze inputs and outputs to functions for refactoring',
    url='https://github.com/frnsys/snp',
    author='Francis Tseng (@frnsys)',
    license='MIT',
    packages=find_packages(),
    install_requires=[
    ]
)
